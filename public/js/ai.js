import{api,escapeHtml}from'./api.js';import{me}from'./auth.js';
export function renderAI(root,notice){if(!me()){root.innerHTML='<p class="empty">Sign in to use the separate AI workspace.</p>';return}const messages=[];root.innerHTML='<div class="ai-wrap"><div class="ai-intro"><strong>Separate AI workspace</strong><br>Your feed is not included. Only messages you submit here go to the configured provider.</div><div class="chat" aria-live="polite"></div><form class="chat-form"><input name="message" aria-label="Message AI" maxlength="4000" placeholder="Write a message…" required><button>Send</button></form></div>';const draw=()=>root.querySelector('.chat').innerHTML=messages.map(m=>`<div class="bubble ${m.role}">${escapeHtml(m.content)}</div>`).join('');root.querySelector('form').onsubmit=async e=>{e.preventDefault();const input=e.target.message,content=input.value.trim();if(!content)return;messages.push({role:'user',content});input.value='';draw();e.target.querySelector('button').disabled=true;try{const r=await api('/api/ai/chat',{method:'POST',body:JSON.stringify({messages})});messages.push({role:'assistant',content:r.message});draw()}catch(x){notice(x.message)}finally{e.target.querySelector('button').disabled=false}}}
