{
  "name": "pikapp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test",
    "check": "node --check worker/index.js && node --check public/js/app.js && node --check public/js/feed.js && node --check public/js/ai.js && node --check public/js/mail.js"
  },
  "dependencies": {
    "minisearch": "^7.1.2"
  },
  "devDependencies": {
    "wrangler": "^4.28.1"
  }
}
