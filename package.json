{
  "name": "pikamail",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test",
    "check": "node --check worker/index.js && node --check public/app.js"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^4.28.1"
  }
}
